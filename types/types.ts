export type StrapiFile = {
   data: {
      attributes: {
         url: string;
         name: string;
      }
   }
}

export type StrapiFileArray = {
   attributes: {
      url: string;
      name: string;
   }
}